import { Action, Actions } from './actionTypes';
import { StoreState, StetaTypes } from '../../types/index';

const initialStatet: StetaTypes = {};

export function reducerName(state: StetaTypes = initialStatet, action: Action): StetaTypes {
  switch (action.type) {
  }
}
